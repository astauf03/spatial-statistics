library(aod)
library(ggplot2)
library(rms)
library(gmodels)
library(nnet)
library(DAAG)
library(ROCR)
library(xtable)
options(scipen=999)
cdc_data <- read.csv("logistic_regression.csv")
cdc_data <- read.csv("data/logistic_regression.csv")
head(cdc_data)
DRINKING_D.tab <- table(mydata$DRINKING_D)
DRINKING_D.tab <- table(cdc_data$DRINKING_D)
prop.table(DRINKING_D.tab)
# I made a function to loop through it so I don't have to do this 7 separate times.
predictors <- c(
"FATAL_OR_M", "OVERTURNED", "CELL_PHONE",
"SPEEDING", "AGGRESSIVE",
"DRIVER1617", "DRIVER65PLUS"
)
for (var in predictors) {
cat("\nCrosstab: DRINKING_D vs", var, "\n")
CrossTable(cdc_data$DRINKING_D, cdc_data[[var]],
prop.r = FALSE,
prop.chisq = FALSE,
chisq = FALSE,
prop.t = FALSE)
}
# I made a function to loop through it so I don't have to do this 7 separate times.
predictors <- c(
"FATAL_OR_M", "OVERTURNED", "CELL_PHONE",
"SPEEDING", "AGGRESSIVE",
"DRIVER1617", "DRIVER65PLUS"
)
for (var in predictors) {
cat("Predictor:", var, "\n")
# Counts.
tab <- table(cdc_data$DRINKING_D, cdc_data[[var]])
print(tab)
# Row percentages (within DRINKING_D)
cat("\nRow percentages (within DRINKING_D):\n")
print(round(prop.table(tab, 1), 4))
# Total number of predictor=1 in entire dataset
cat("\nTotal", var, "= 1 in full dataset:", sum(cdc_data[[var]] == 1), "\n")
cat("\nCROSSTABLE OUTPUT:\n")
CrossTable(cdc_data$DRINKING_D,
cdc_data[[var]],
prop.r = FALSE,
prop.chisq = FALSE,
chisq = FALSE,
prop.t = FALSE)
}
library(aod)
library(ggplot2)
library(rms)
library(gmodels)
library(nnet)
library(DAAG)
library(ROCR)
library(xtable)
options(scipen = 999)
# I made a function to loop through it so I don't have to do this 7 separate times.
predictors <- c(
"FATAL_OR_M", "OVERTURNED", "CELL_PHONE",
"SPEEDING", "AGGRESSIVE",
"DRIVER1617", "DRIVER65PLUS"
)
for (var in predictors) {
cat("Predictor:", var, "\n")
# Counts.
table_counts <- table(cdc_data$DRINKING_D, cdc_data[[var]])
print(table_counts)
# Row percentages within DRINKING_D.
cat("\nRow percentages within DRINKING_D:\n")
print(round(prop.table(table_counts, 1), 4))
# Total number of predictors = 1 in the entire dataset.
cat("\nTotal", var, "= 1 in full dataset:", sum(cdc_data[[var]] == 1), "\n")
cat("\nCROSSTABLE OUTPUT:\n")
CrossTable(cdc_data$DRINKING_D,
cdc_data[[var]],
prop.r = FALSE,
prop.chisq = FALSE,
chisq = FALSE,
prop.t = FALSE)
}
# I made a function to loop through it so I don't have to do this 7 separate times.
predictors <- c(
"FATAL_OR_M", "OVERTURNED", "CELL_PHONE",
"SPEEDING", "AGGRESSIVE",
"DRIVER1617", "DRIVER65PLUS"
)
for (var in predictors) {
cat("Predictor:", var, "\n")
# Counts.
table_counts <- table(cdc_data$DRINKING_D, cdc_data[[var]])
print(table_counts)
# Row percentages within DRINKING_D.
cat("\nRow percentages within DRINKING_D:\n")
print(round(prop.table(table_counts, 1), 4))
# Total number of predictors = 1 in the entire dataset.
cat("\nTotal", var, "= 1 in full dataset:", sum(cdc_data[[var]] == 1), "\n")
cat("\nCROSSTABLE OUTPUT:")
CrossTable(cdc_data$DRINKING_D,
cdc_data[[var]],
prop.r = FALSE,
prop.chisq = FALSE,
chisq = FALSE,
prop.t = FALSE)
}
# I made a function to loop through it so I don't have to do this 7 separate times.
predictors <- c(
"FATAL_OR_M", "OVERTURNED", "CELL_PHONE",
"SPEEDING", "AGGRESSIVE",
"DRIVER1617", "DRIVER65PLUS"
)
for (var in predictors) {
cat("PREDICTOR:", var, "\n")
# Counts.
table_counts <- table(cdc_data$DRINKING_D, cdc_data[[var]])
print(table_counts)
# Row percentages within DRINKING_D.
cat("\nROW % WITHIN DRINKING_D:\n")
print(round(prop.table(table_counts, 1), 4))
# Total number of predictors = 1 in the entire dataset.
cat("\nTOTAL", var, "= 1 IN FULL DATASET:", sum(cdc_data[[var]] == 1), "\n")
cat("\nCROSSTABLE OUTPUT:")
CrossTable(cdc_data$DRINKING_D,
cdc_data[[var]],
prop.r = FALSE,
prop.chisq = FALSE,
chisq = FALSE,
prop.t = FALSE)
}
# I made a function to loop through it so I don't have to do this 7 separate times.
predictors <- c(
"FATAL_OR_M", "OVERTURNED", "CELL_PHONE",
"SPEEDING", "AGGRESSIVE",
"DRIVER1617", "DRIVER65PLUS"
)
for (var in predictors) {
cat("\n======\n")
cat("PREDICTOR:", var, "\n")
# Counts.
table_counts <- table(cdc_data$DRINKING_D, cdc_data[[var]])
print(table_counts)
# Row percentages within DRINKING_D.
cat("\nROW % WITHIN DRINKING_D:\n")
print(round(prop.table(table_counts, 1), 4))
# Total number of predictors = 1 in the entire dataset.
cat("\nTOTAL", var, "= 1 IN FULL DATASET:", sum(cdc_data[[var]] == 1), "\n")
cat("\nCROSSTABLE OUTPUT:")
CrossTable(cdc_data$DRINKING_D,
cdc_data[[var]],
prop.r = FALSE,
prop.chisq = FALSE,
chisq = FALSE,
prop.t = FALSE)
}
# I made a function to loop through it so I don't have to do this 7 separate times.
predictors <- c(
"FATAL_OR_M", "OVERTURNED", "CELL_PHONE",
"SPEEDING", "AGGRESSIVE",
"DRIVER1617", "DRIVER65PLUS"
)
for (var in predictors) {
cat("\n==============================\n")
cat("PREDICTOR:", var, "\n")
# Counts.
table_counts <- table(cdc_data$DRINKING_D, cdc_data[[var]])
print(table_counts)
# Row percentages within DRINKING_D.
cat("\nROW % WITHIN DRINKING_D:\n")
print(round(prop.table(table_counts, 1), 4))
# Total number of predictors = 1 in the entire dataset.
cat("\nTOTAL", var, "= 1 IN FULL DATASET:", sum(cdc_data[[var]] == 1), "\n")
cat("\nCROSSTABLE OUTPUT:")
CrossTable(cdc_data$DRINKING_D,
cdc_data[[var]],
prop.r = FALSE,
prop.chisq = FALSE,
chisq = FALSE,
prop.t = FALSE)
}
# I made a function to loop through it so I don't have to do this 7 separate times.
predictors <- c(
"FATAL_OR_M", "OVERTURNED", "CELL_PHONE",
"SPEEDING", "AGGRESSIVE",
"DRIVER1617", "DRIVER65PLUS"
)
for (var in predictors) {
cat("\n============================================================\n")
cat("PREDICTOR:", var, "\n")
# Counts.
table_counts <- table(cdc_data$DRINKING_D, cdc_data[[var]])
print(table_counts)
# Row percentages within DRINKING_D.
cat("\nROW % WITHIN DRINKING_D:\n")
print(round(prop.table(table_counts, 1), 4))
# Total number of predictors = 1 in the entire dataset.
cat("\nTOTAL", var, "= 1 IN FULL DATASET:", sum(cdc_data[[var]] == 1), "\n")
cat("\nCROSSTABLE OUTPUT:")
CrossTable(cdc_data$DRINKING_D,
cdc_data[[var]],
prop.r = FALSE,
prop.chisq = FALSE,
chisq = FALSE,
prop.t = FALSE)
}
# I made a function to loop through it so I don't have to do this 7 separate times.
predictors <- c(
"FATAL_OR_M", "OVERTURNED", "CELL_PHONE",
"SPEEDING", "AGGRESSIVE",
"DRIVER1617", "DRIVER65PLUS"
)
for (var in predictors) {
cat("============================================================\n")
cat("PREDICTOR:", var, "\n")
# Counts.
table_counts <- table(cdc_data$DRINKING_D, cdc_data[[var]])
print(table_counts)
# Row percentages within DRINKING_D.
cat("\nROW % WITHIN DRINKING_D:\n")
print(round(prop.table(table_counts, 1), 4))
# Total number of predictors = 1 in the entire dataset.
cat("\nTOTAL", var, "= 1 IN FULL DATASET:", sum(cdc_data[[var]] == 1), "\n")
cat("\nCROSSTABLE OUTPUT:")
CrossTable(cdc_data$DRINKING_D,
cdc_data[[var]],
prop.r = FALSE,
prop.chisq = FALSE,
chisq = FALSE,
prop.t = FALSE)
}
# I made a function to loop through it so I don't have to do this 7 separate times.
predictors <- c(
"FATAL_OR_M", "OVERTURNED", "CELL_PHONE",
"SPEEDING", "AGGRESSIVE",
"DRIVER1617", "DRIVER65PLUS"
)
for (var in predictors) {
cat("============================================================\n")
cat("PREDICTOR:", var, "\n")
# Counts.
table_counts <- table(cdc_data$DRINKING_D, cdc_data[[var]])
print(table_counts)
# Row percentages within DRINKING_D.
cat("\nROW % WITHIN DRINKING_D:\n")
print(round(prop.table(table_counts, 1), 4))
# Total number of predictors = 1 in the entire dataset.
cat("\nTOTAL", var, "= 1 IN FULL DATASET:", sum(cdc_data[[var]] == 1), "\n")
cat("\nCROSSTABLE OUTPUT:")
CrossTable(cdc_data$DRINKING_D,
cdc_data[[var]],
prop.r = FALSE,
prop.t = FALSE,
prop.chisq = FALSE,
chisq = TRUE)
}
